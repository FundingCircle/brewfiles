#!/bin/bash

if [[ -z $(which brew) ]]; then
  echo 'Unable to find brew, please install it from http://brew.sh'
  open 'http://brew.sh'
  exit 1
fi

source incorrect_cassandra_version_check.sh

brew update
brew upgrade
brew tap Homebrew/brewdler
brew brewdle
brew cleanup
